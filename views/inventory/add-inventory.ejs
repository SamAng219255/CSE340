<h1><%= title %></h1>

<%- messages() %>

<div id="login-box">
  <% if (errors) { %>
    <ul class="error-notice">
   <% errors.array().forEach(error => { %>
     <li><%= error.msg %></li>
  <%  }) %>
   </ul>
  <% } %>
  <form action="/inv/add/vehicle" method="post">
    <label>
      Make
      <input type="text" name="inv_make" value="<%= locals.inv_make %>" required>
      <span class="required-note">*Required.</span>
    </label>
    <label>
      Model
      <input type="text" name="inv_model" value="<%= locals.inv_model %>" required>
      <span class="required-note">*Required.</span>
    </label>
    <label>
      Year
      <input type="number" name="inv_year" pattern="[0-9]{4}" value="<%= locals.inv_year %>" required>
      <span class="required-note">*Required. Must be a four (4) digit year.</span>
    </label>
    <label>
      Description
      <textarea type="text" name="inv_description" required><%= locals.inv_description %></textarea>
      <span class="required-note">*Required.</span>
    </label>
    <label>
      Image Path
      <input type="text" name="inv_image" value="<%- locals.inv_image %>" required>
      <span class="required-note">*Required.</span>
    </label>
    <label>
      Thumbnail Path
      <input type="text" name="inv_thumbnail" value="<%- locals.inv_thumbnail %>" required>
      <span class="required-note">*Required.</span>
    </label>
    <label>
      Price
      <input type="number" name="inv_price" pattern="[0-9]+" value="<%= locals.inv_price %>" required>
      <span class="required-note">*Required. Must be a whole number of dollars.</span>
    </label>
    <label>
      Miles
      <input type="number" name="inv_miles" pattern="[0-9]+" value="<%= locals.inv_miles %>" required>
      <span class="required-note">*Required. Must be a whole number of miles.</span>
    </label>
    <label>
      Color
      <input type="text" name="inv_color" value="<%= locals.inv_color %>" required>
      <span class="required-note">*Required.</span>
    </label>
    <label>
      Classification
      <select name="classification_id" id="classification_id" required>
        <%- locals.classificationOptions %>
      </select>
    </label>
    <input type="submit" value="Add Item" disabled>
  </form>
</div>